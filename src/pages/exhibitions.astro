---
import BaseLayout from '../layouts/BaseLayout.astro';
---
<BaseLayout title="Exhibitions" description="The gallery track — exhibition history and the credential." register="art">
  <h1>Exhibitions</h1>
  <p class="stub">Stub. The 2015–2024 exhibition history lives in the current
    Squarespace page's text — migrate it here as the credential record. Candidate
    for a content collection (year, venue, title) if the list gets long.</p>
</BaseLayout>
