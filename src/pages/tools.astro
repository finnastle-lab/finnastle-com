---
import BaseLayout from '../layouts/BaseLayout.astro';

// On-territory hub. Each tool is a specimen of "machines that make."
const tools = [
  {
    name: 'un-write',
    tag: 'Live tool',
    href: 'https://finnastle-lab.github.io/un-write/',
    repo: 'https://github.com/finnastle-lab/un-write',
    blurb:
      'A Hemingway-style editor that flags AI writing tells inline. Paste prose, get colour-coded highlights and a visible “how AI does this read” score. The joke it owns: a tool that rewrites your text to sound less like AI is an AI rewriting your text. Detection, obsolete by design.',
  },
  {
    name: 'fa-art-namer',
    tag: 'How-to + code',
    href: '/tools/fa-art-namer',
    internal: true,
    blurb:
      'A read-only pipeline for cataloguing an art element library — dimensions, transparency, duplicates, keyword-soup names — so a machine can help name and dedupe what a human made. A walk-through with copyable code.',
  },
  {
    name: 'FA Brand Reference',
    tag: 'Design system',
    href: '/tools/design-system/',
    internal: true,
    blurb:
      'The monochrome-first brand system behind this site: the palette, three voices in tension, marks & motifs, and structural patterns.',
  },
  {
    name: 'Icon Pack',
    tag: 'Element library',
    href: '/tools/icon-pack',
    internal: true,
    blurb:
      'Page two of the brand reference: the scanned collage elements, processed to production-ready transparent cut-outs — the raw material of the work.',
  },
];
---
<BaseLayout title="Tools" description="Small tools — specimens of making when machines can make." register="art">
  <h1>Tools</h1>
  <p>Small machines that make, and the methods behind them. On-territory by construction.</p>

  <div class="tool-list">
    {tools.map((t) => (
      <article class="tool">
        <h2>
          <a href={t.href}>{t.name}</a>
          <span class="tag">{t.tag}</span>
        </h2>
        <p>{t.blurb}</p>
        <p class="tool-links">
          <a href={t.href}>{t.internal ? 'Open →' : 'Open the tool →'}</a>
          {t.repo && <> · <a href={t.repo}>Source</a></>}
        </p>
      </article>
    ))}
  </div>
</BaseLayout>
