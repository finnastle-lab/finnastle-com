---
title: "Placeholder — the first essay goes here"
description: "A scaffolding stub so the writing index, RSS feed, and essay route all render. Delete once real essays land."
pubDate: 2026-08-27
draft: true
---

This is a placeholder essay. It exists only so the `/writing` index, the
`/writing/[slug]` route, and the RSS feed have something to render during
scaffolding. It's marked `draft: true`, so it won't appear in the published
list or the feed.

Replace this with the real thing — dictate → write → narrate — when the
cascade produces its first essay.
